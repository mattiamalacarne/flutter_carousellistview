name: carousel_listview
description: "Scroll view implementation with carousel and snap"
version: 1.0.0
homepage: http://broho.mattiamalacarne.com

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
